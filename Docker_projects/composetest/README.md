To start the project you muse be in the folder where compose.yaml is created:

```
docker compose up -d
```

To stop/destroy the project you muse be in the folder where compose.yaml is created:

```
docker compose down
```

To stop/destroy the project and deleta all images build:

```
docker compose down --rmi all
```
